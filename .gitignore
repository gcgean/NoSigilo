# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist_new
dist_chk
dist_old
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Estado do gerador de SEO: guarda o hash e a data da ultima mudanca real de
# cada pagina, para o lastmod do sitemap nao carimbar "mudou hoje" toda vez
# que a rotina roda. E estado de cada maquina, nao codigo — versionar faria a
# rotina do servidor sujar a arvore e brigar com o git pull.
scripts/seo-lastmod.json
