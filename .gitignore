# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist_new
dist_chk
dist_old
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Espelho local do estado do gerador de SEO (hash e data da ultima mudanca de
# cada pagina, que sustentam o lastmod do sitemap). A copia que vale fica na
# tabela seo_lastmod do Postgres; este arquivo so entra em cena quando o banco
# nao responde. Fora do git porque a rotina do cron o reescreve toda madrugada
# e sujaria a arvore antes do proximo git pull.
scripts/seo-lastmod.json
